# Approves the build scripts of these dependencies so `pnpm install` does not
# block them. Generated by `pnpm approve-builds`; `allowBuilds` is the key
# recognized by pnpm 11 (the version used locally).
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  lmdb: true
  msgpackr-extract: true
